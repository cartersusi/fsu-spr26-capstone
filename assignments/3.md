## Architecture & Formatting (End-User)
Determine how the files should be organized for the user. 
Example:
Dashcam/Videos/MyClips/clip_{datetime}

Determine the file format, disk format, and video encoding most compatible with Mac, Windows, Chromebooks.


## Python GUI
**Display**: `640x480px`

Main Display:
- Clip Button
- Settings Button
- Warning icons

### Settings
- Brightness Slider
- Clip Settings
  - Duration
  - Quality
- Auto-Clip Settings
  - Duration 
  - Quality
  - Sudden Stops dis/enable
  - Warnings  dis/enable

## Scripts/Programs
List of scripts/programs we need to make for the dashcam

### Light Level Detection
Load in images and be able to determine how much light is available. E.g. if it is morning, day time, afternoon, or night. 

### Image Caching
An image cache to store the previous frames. The size is configured from the GUI settings. 
Each clipping service should be able to pull from the same cache. 

### Lane detection (maybe)
Haven’t looked into how expensive this is and we’re already using more power than expected now that we have a screen. 

Be able to read the lane the user is currently in.